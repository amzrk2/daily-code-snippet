/**
 * 不支持冒泡的事件
 *
 * mouseenter
 * mouseleave
 * load
 * unload
 * focus
 * blur
 * resize
 */

typeof null === 'object';
typeof {} === 'object';
typeof [] === 'object';
typeof Symbol() === 'symbol';
typeof 123n === 'bigint';
typeof (() => {}) === 'function';
